# Análisis sentimental de patologías relacionadas con la salud mental

Para preprocesar los datos es necesario ejecutar los siguiente commandos

```bash
pip install -r requirements.txt
python preprocess.py
```